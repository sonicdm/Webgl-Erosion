/**
 * Configuration for Three.js runtime
 * Set USE_THREEJS_RUNTIME to true to use Three.js instead of WebGL pipeline
 */

export const THREEJS_CONFIG = {
  // Set to true to use Three.js runtime, false to use WebGL pipeline
  USE_THREEJS_RUNTIME: true,
  
  // Enable extension validation (will throw error if missing)
  VALIDATE_EXTENSIONS: true,
};

