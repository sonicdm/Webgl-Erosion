---
description: Use test:ci and PowerShell syntax when running terminal commands from the Cursor agent
alwaysApply: true
---

# Running Tests and Build from the Cursor Agent

When running terminal commands from the Cursor agent (run_terminal_cmd / Shell), use:

- **Shell:** **PowerShell syntax only.** This environment uses PowerShell on Windows. Prefer PowerShell over Bash/Unix-style.
  - Env: `$env:VAR = "value"` not `export VAR=value`
  - Remove: `Remove-Item -Recurse -Force path` or `rm -r -fo path`; not `rm -rf path`
  - Chain: `;` or `&&` (PowerShell 7+). Prefer `;` for compatibility
  - Paths: `/` and `\` both work; use `\` or `Join-Path` when scripting

- **Tests:** `npm run test:ci` (not `npm test`)

`npm test` runs Jest with parallel workers (`child_process.fork`). In the Cursor agent execution environment this can fail with `spawn EPERM`. `test:ci` uses `jest --runInBand` (single process, no workers) and runs correctly.

- **Build:** `npm run build` — use as usual.

- **Local/terminal:** `npm test` works; use it. `test:ci` is only for agent/CI environments where `npm test` fails.
